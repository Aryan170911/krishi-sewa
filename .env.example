# Krishi Sewa - Environment Variables
# Copy to .env and fill values

# Server
PORT=3000
NODE_ENV=production
CORS_ORIGIN=*

# Admin (change in production!)
ADMIN_USER=admin
ADMIN_PASS=admin123
ADMIN_TOKEN=krishi-admin-token-2026

# Supabase (Postgres) — for production persistence (else JSON files)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Payment Gateway - Razorpay (test keys, replace for prod)
RAZORPAY_KEY_ID=rzp_test_Sv4HWH1qFfP22s
RAZORPAY_KEY_SECRET=test_secret_for_demo

# Python (same vars, read via os.environ)
# FLASK_ENV=production

# Database (future: switch from JSON to Postgres/Mongo)
# DATABASE_URL=postgres://user:pass@host:5432/krishi
# MONGO_URL=mongodb://localhost:27017/krishi
