services:
  - type: web
    name: krishi-sewa
    env: node
    plan: free
    buildCommand: npm install --prefix server
    startCommand: node server/server.js
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
