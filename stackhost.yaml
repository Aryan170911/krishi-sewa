user:
  name: "Aryan"
  github: "Aryan170911"

runtime:
  image: "python:3.11-slim"

commands:
  package: ""
  build:
    - "pip install -r requirements.txt"
  start: "gunicorn api:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"

repository:
  branch: "main"
  auto_deploy: true
