user:
  name: "Aryan"
  github: "Aryan170911"

runtime:
  image: "python:3.11-slim"

commands:
  package: "nodejs npm"
  build:
    - "npm install --prefix server"
  start: "node server/server.js"

repository:
  branch: "main"
  auto_deploy: true
